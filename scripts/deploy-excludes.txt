# rsync --exclude-from (пути от корня репозитория). С --delete эти пути на сервере
# НЕ удаляются и НЕ перезаписываются. ВНИМАНИЕ: без правильных исключений --delete
# может стереть БД/секреты/чужие сайты.
#
# ВАЖНО: комментарии — ТОЛЬКО на отдельной строке (с #). Инлайн-комментарий после
# паттерна rsync считает частью пути — исключение перестаёт работать.

# --- ДАННЫЕ И БД (никогда не трогать) ---
# /data/ = runtime + том Postgres (data/pg) — КРИТИЧНО
/data/

# --- Загрузки из админок ---
/public/assets/img/menu/uploads/
/public/assets/img/gallery/uploads/
/public/assets/img/vip/partner_uploads/
/public/assets/img/hall/uploads/
/public/assets/img/vitrina/uploads/

# --- Секреты и dev-only ---
/.env
# локальные доступы к серверу для deploy.sh
/scripts/deploy.env
# сертификаты живут на сервере, не в git
/docker/ssl/
# локальный dev-override (переводит стек в dev-режим — на прод нельзя)
/docker/docker-compose.override.yml
# dev-vhost
/docker/nginx/dev/
/docker/php/Dockerfile.dev

# --- Соседний арендатор (вне этого репо) ---
/miraipro_site/

# --- Мусор сборки / локальное ---
# vendor собирается на сервере в образе php (Dockerfile composer install)
/vendor/
/.git/
/.github/
/node_modules/
/frontend/node_modules/
/assets-doc/
/graphify-out/
/.playwright-mcp/
/.claude/
/var/
/composer.phar
*.png
.DS_Store
